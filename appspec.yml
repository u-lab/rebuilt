version: 0.0
os: linux
files:
  - source: /
    destination: /home/ec2-user/rebuilt
permissions:
  - object: /home/ec2-user/rebuilt
    owner: ec2-user
    group: ec2-user
    mode: 700
hooks:
   ValidateService:
     - location: scripts/init.sh
       timeout: 600
